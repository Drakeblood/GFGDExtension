<?xml version="1.0" encoding="UTF-8" ?>
<class name="WaterVolume2D" inherits="Area2D" api_type="extension" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/godotengine/godot/master/doc/class.xsd">
	<brief_description>
		A body of water: put one anywhere a character should swim.
	</brief_description>
	<description>
		The 2D twin of [WaterVolume]. An [Area2D], so its shape is authored the way any trigger volume is.
		[CharacterMovementComponent2D] does not connect to its signals - it asks the physics server which volume is at a point, every tick, from the transform it is simulating. Signals would be state accumulated outside the simulation, and a replayed tick would see whatever the last enter or exit happened to leave behind rather than what was true at that tick.
	</description>
	<tutorials>
	</tutorials>
	<members>
		<member name="buoyancy" type="float" setter="set_buoyancy" getter="get_buoyancy" default="1.0">
			How strongly the water cancels gravity. [code]1.0[/code] is neutral: a submerged character neither sinks nor rises. Below 1 it sinks, above 1 it floats up to the surface.
		</member>
		<member name="monitoring" type="bool" setter="set_monitoring" getter="is_monitoring" overrides="Area2D" default="false" />
		<member name="water_velocity" type="Vector2" setter="set_water_velocity" getter="get_water_velocity" default="Vector2(0, 0)">
			A current, in pixels per second. Added to whatever the character is doing, so it drifts even while still.
		</member>
	</members>
</class>
